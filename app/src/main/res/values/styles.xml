<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <style name="StartBtn" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">@dimen/width_startBtn</item>
        <item name="android:layout_height">@dimen/height_startBtn</item>
        <item name="android:text">@string/start</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/bg_start_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_1</item>
    </style>

    <style name="SnakeImg">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="offsetMargin">@dimen/margin_snake_ver</item>
    </style>

    <style name="MySnakeImg" parent="SnakeImg">
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_marginStart">@dimen/margin_snake_hor</item>
        <item name="android:layout_marginBottom">@dimen/margin_snake_ver</item>
        <item name="blinkImage">@drawable/snake_my_blink</item>
        <item name="pressedImage">@mipmap/snake_my_die</item>
        <item name="direction">0</item>
    </style>

    <style name="EnemySnakeImg" parent="SnakeImg">
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:layout_marginEnd">@dimen/margin_snake_hor</item>
        <item name="android:layout_marginTop">@dimen/margin_snake_ver</item>
        <item name="blinkImage">@drawable/snake_enemy_blink</item>
        <item name="pressedImage">@mipmap/snake_enemy_die</item>
        <item name="direction">2</item>
    </style>

</resources>
